import Foundation

/// Public surface of the KuwentaCore package. Kept intentionally tiny in
/// Phase 1 so the iOS app target compiles cleanly. Phase 2 will extend this
/// with the Message Filter parser pipeline.
public enum KuwentaCore {
    public static let version = "0.1.0"
}
